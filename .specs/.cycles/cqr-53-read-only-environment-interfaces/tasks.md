# Tasks — Read-only Environment Interfaces

> Build order follows the dependency chain **1 → 2 → (3) → (4, 5) → 6 → 7**. Tasks read
> [`requirements.md`](./requirements.md) (R1–R6) and [`plan.md`](./plan.md) in this
> folder. Mark each `[-]` when you start and `[x]` when done.
>
> **Reactivity is the gating risk:** Task 2 must prove live per-tick updates before the
> art arms (4, 5) are built. The 3D arm (5) is **cut-able** — if the 2D arm (4) already
> feels right, skip to Task 6 and record the cut.

- [x] 1. View-model contract + pure mappers
  - Files: `src/modules/environment-view/types.ts` (new), `src/modules/environment-view/environment-view.ts` (new)
  - Define `WorkerView`, `WorkerStatus`, `InventoryRow`, `EnvironmentView`, `CityBuildingSummary`, `CityView` per plan.md § types.ts. Implement `resolveTaskLabel`, `workerProgress`, `mapWorker`, `mapEnvironmentView`, `mapCityView` per plan.md § environment-view.ts. No Vue, no `three`.
  - _Leverage: `src/modules/items/registry.ts` (ItemRegistry name/value), `src/game/city/buildings/common/Building.ts`, `src/game/city/buildings/common/Worker.ts`, `src/game/city/buildings/common/Action.ts`, `src/game/city/City.ts`_
  - _Requirements: R1, R6_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle. First read requirements.md and plan.md in `.specs/.cycles/cqr-53-read-only-environment-interfaces/`. Role: TypeScript developer (domain/presentation mapping) | Task: Create the two files. In `types.ts` export the DTO interfaces exactly as in plan.md § Components → types.ts. In `environment-view.ts` implement: `resolveTaskLabel(action) = action ? action.constructor.name : null` (raw identifier — do NOT read `action.name`, it is undefined for Blacksmith actions); `workerProgress(action)` returns 0 when action is null or `action.isDone()`, else `Math.min(1, Math.max(0, 1 - ticks_remaining/total_ticks))` guarded so a non-finite result (e.g. total_ticks 0, or the 999 pre-start sentinel) yields 0; `mapWorker(worker,index,label)` → status `'working'` iff `worker.active_action && !worker.active_action.isDone()` else `'idle'`; `mapEnvironmentView(building)` reads `building.static.name`, `building.money`, maps `building.workers` with an index label (per-building prefix map, e.g. BlackSmith→"Smith"), and maps `building.inventory.getCountByGoodId()` to `InventoryRow[]` via `ItemRegistry`; `mapCityView(city)` reads `city.money`, `city.citizens_count`, and a minimal `CityBuildingSummary[]` from `city.buildings`. | Restrictions: Pure functions only — no Vue, no `three`, no mutation of any engine object, no side effects. | _Leverage: ItemRegistry, BaseBuilding/Worker/Action/City read APIs_ | _Requirements: R1, R6_ | Success: `vue-tsc -b` passes; constructing a `MakeIngotAction` and passing its worker yields task `"MakeIngot"`; an idle worker yields `{task:null, progress:0, status:'idle'}`; a started action yields progress in [0,1]. Mark this task [-] before starting and [x] when done._

- [x] 2. Reactivity composable + live-update gate
  - File: `src/components/environment/useEnvironmentView.ts` (new)
  - Implement `useEnvironmentView(buildingId: Ref<BuildingID|null>)` and `useCityView()` using the `reactive(GameControllerSingleton)` + `controller.tick` heartbeat pattern from plan.md. Then **prove** live updates (temporary mount or console harness) before proceeding.
  - _Leverage: `src/App.vue:64` (reactive(GameControllerSingleton)), `src/App.vue:35` (c.tick), `src/game/controllers/GameController.ts`, the mappers from Task 1_
  - _Requirements: R1.7, NFR Reactivity_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle (read requirements.md + plan.md first). Role: Vue 3 reactivity specialist | Task: Create `useEnvironmentView.ts`. `useEnvironmentView(buildingId)` returns a `computed` that reads `void controller.tick` (heartbeat), resolves the building fresh via `controller.city.buildings.get(buildingId.value)` (null-safe), and returns `mapEnvironmentView(building)` or null. `useCityView()` returns a `computed` reading the heartbeat and `mapCityView(controller.city)`. Use `const controller = reactive(GameControllerSingleton) as GameController` exactly like App.vue. Then verify live updates: temporarily render the value (or log it from a tiny harness) while ticking the game and confirm worker progress / inventory change every tick without refresh. | Restrictions: Read-only; do not mutate controller/city/buildings. Do not introduce a store. If the heartbeat alone does not produce updates, diagnose before building any art arm. | _Leverage: App.vue reactive pattern, Task 1 mappers_ | _Requirements: R1.7, NFR Reactivity_ | Success: A bound computed visibly updates each tick (manual Next-Tick and auto-run). Document the verification result in your completion note. Mark [-]/[x] in this file._

- [x] 3. Container + registry + generic fallback, wired into App.vue (replaces the `<pre>` dump)
  - Files: `src/components/environment/environment-registry.ts` (new), `src/components/environment/EnvironmentView.vue` (new), `src/components/environment/GenericEnvironmentView.vue` (new), `src/App.vue` (modify)
  - Registry maps `BuildingID → art component` (only BlackSmith for now, async-imported). Container dispatches: null id → city-global 3D (lazy; until Task 5 use a placeholder), registered building → art view, else → `GenericEnvironmentView`. Replace `<pre v-else>` at App.vue:30 with `<EnvironmentView :building-id="active_building_id" />`.
  - _Leverage: `src/App.vue:25-30` (Market v-if / pre v-else), `src/components/buildings/MarketPanel.vue` (read-only panel precedent), Task 1 + Task 2 outputs_
  - _Requirements: R2, R6_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle (read requirements.md + plan.md first). Role: Vue 3 TypeScript developer | Task: 1) `environment-registry.ts`: export `environmentArtRegistry: Partial<Record<BuildingID, Component>>` with BlackSmith → `defineAsyncComponent(() => import('./views/BlacksmithView2D.vue'))`. 2) `GenericEnvironmentView.vue`: prop `view: EnvironmentView | null`; render name, funds, a worker list (label · raw task · progress bar) and an inventory table with Tailwind only (no SVG/3D). 3) `EnvironmentView.vue`: prop `buildingId: BuildingID | null`; if null render the 3D city view (lazy `CityGlobalView3D`, bound to `useCityView()` — until Task 5 lands, render a simple placeholder div showing city money/citizens); if a registry entry exists render it bound to `useEnvironmentView(toRef(props,'buildingId'))`; else render `GenericEnvironmentView` with the same view-model. 4) In `App.vue`, replace `<pre v-else>{{ activeBuilding }}</pre>` (line 30) with `<EnvironmentView v-else :building-id="active_building_id" />` and import it; leave the `MarketPanel` `v-if` branch untouched. | Restrictions: Read-only; the Market path must not change. No per-building if/else in the container beyond the registry lookup + the three documented fallbacks. | _Leverage: App.vue structure, MarketPanel pattern, Tasks 1–2_ | _Requirements: R2, R6_ | Success: `vue-tsc -b` passes; the raw `<pre>` dump is gone; selecting IronMine/LumberMill shows the generic view; the Market still shows MarketPanel with controls; no active tab shows the city placeholder/scene. Mark [-]/[x]._

- [x] 4. 2D Blacksmith art prototype (SVG/CSS)
  - Files: `src/components/environment/views/BlacksmithView2D.vue` (new) (registered in Task 3)
  - SVG/CSS blacksmith scene: 2 worker figures (Smith 1 / Smith 2) each with raw task label + progress indicator, an inventory area, and a funds readout. Consumes `useEnvironmentView`. Strictly read-only; live-updating.
  - _Leverage: Task 1 `EnvironmentView` DTO, Task 2 `useEnvironmentView`, Tailwind, `src/components/buildings/MarketPanel.vue` (read-only layout reference)_
  - _Requirements: R3, R6_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle (read requirements.md + plan.md first). Role: Vue 3 + SVG/CSS UI developer | Task: Create `BlacksmithView2D.vue`. Prop `view: EnvironmentView | null`. Render an SVG (or styled HTML/CSS) blacksmith interior: one figure per `view.workers` (expect 2 — "Smith 1"/"Smith 2"), each showing the raw `worker.task` label (show "idle" when null) and a progress bar/ring bound to `worker.progress` (0..1); an inventory area listing `view.inventory` rows (name + count, optionally unit value); and a funds readout from `view.funds`. Must update live as the prop changes. | Restrictions: Read-only — no buttons that mutate state. Read ONLY from the `view` prop, never from engine singletons. Raw task identifiers (no humanization). Keep it thin. | _Leverage: EnvironmentView DTO, useEnvironmentView, Tailwind_ | _Requirements: R3, R6_ | Success: Selecting the Blacksmith shows 2 workers with live task + progress, live inventory, and funds; a 5-second glance answers who/what/progress/stock/funds without reading code; `vue-tsc -b` passes. Mark [-]/[x]._

- [x] 5. 3D city-global art prototype (Three.js/TresJS) — cut-able
  - Files: `src/components/environment/views/CityGlobalView3D.vue` (new), `package.json` (add deps)
  - Add `three` + `@tresjs/core`. Build a thin `<TresCanvas>` scene placing one simple mesh per `CityView.buildings[]`, with a money + citizens overlay; bound to `useCityView()`; lazy-loaded via the container. **Cut rule:** if Task 4 already feels right, skip this and document the cut in Task 6. **As-built:** *not* cut — this arm was extended into the grid-based [`3d-city-grid`](./3d-city-grid/tasks.md) sub-feature (grid map, port + storage, debug grid); the on-canvas money/citizens overlay was later removed in favour of the top bar.
  - _Leverage: Task 1 `CityView` DTO, Task 2 `useCityView`, `src/game/controllers/GameController.ts` (`isNight()` for optional ambient), TresJS docs_
  - _Requirements: R4, R6_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle (read requirements.md + plan.md first). Role: Vue 3 + Three.js/TresJS developer | Task: 1) `npm install three @tresjs/core` (add to dependencies). 2) Create `CityGlobalView3D.vue`, prop `view: CityView`: a `<TresCanvas>` with a simple camera/light and one basic mesh per `view.buildings` entry (the city "in the background"), plus an HTML overlay showing `view.money` and `view.citizens`. Optionally tint the light by `GameController.isNight()` (read-only). Keep it minimal. 3) Confirm the container (Task 3) lazy-loads this so `three` is not in the 2D/initial bundle. | Restrictions: Read-only. `three`/`@tresjs/core` may be imported ONLY in this file. Do not block or slow the tick loop. If cutting the arm instead, leave a minimal placeholder and note it in Task 6 — do not half-build. | _Leverage: CityView DTO, useCityView, isNight()_ | _Requirements: R4, R6_ | Success: With no tab selected the city renders as a 3D backdrop with money/citizens overlay and smooth frame rate; uninstalling the deps still lets the 2D path build (proves isolation); `vue-tsc -b` passes. OR: the arm is cut and the decision is recorded (Task 6). Mark [-]/[x]._

- [x] 6. 2D-vs-3D comparison write-up + rollout follow-ups
  - File: `.specs/features/environment-interfaces/2d-vs-3d-decision.md` (new)
  - Compare the arms on effort, feel, performance, maintainability; state one recommended art direction; enumerate rollout follow-up cycles (IronMine, LumberMill, city-global; Market excluded). If the 3D arm was cut, record why and what was learned.
  - _Leverage: the implemented Tasks 4 (and 5, or the cut decision), `.specs/features/interface-controls/README.md` (the milestone this supersedes), `.specs/discovery-next-feature-2026-06-08.md` (X2)_
  - _Requirements: R5_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle (read requirements.md + plan.md first). Role: Tech lead / technical writer | Task: Create `.specs/features/environment-interfaces/2d-vs-3d-decision.md`. Sections: Context (what was built); Comparison table (effort | feel | performance | maintainability for 2D vs 3D); Recommendation (one chosen art direction with rationale); Rollout follow-ups (one cycle each for IronMine, LumberMill, city-global in the chosen style; note Market is excluded). If the 3D arm was cut before completion, say so explicitly and capture the reasoning and any partial findings. Note that this supersedes the "Intended next UI milestone" in interface-controls/README.md. | Restrictions: Base claims on what was actually built/measured, not speculation. Keep it canonical and concise. | _Leverage: Tasks 4/5 outcomes, interface-controls README, discovery X2_ | _Requirements: R5_ | Success: A canonical `.specs/` doc exists with a clear recommendation and enumerated rollout cycles. Mark [-]/[x]._

- [x] 7. Verify end-to-end (`vue-tsc -b` + manual checklist)
  - Run the `build` typecheck and the dev server; exercise the manual verification checklist in plan.md § Testing Strategy.
  - _Leverage: plan.md § Testing Strategy, `npm run build` (vue-tsc -b), `npm run dev`_
  - _Requirements: All_
  - _Prompt: Implement this task for the read-only-environment-interfaces cycle (read requirements.md + plan.md first). Role: QA / TypeScript developer | Task: Run `npm run build` (vue-tsc -b) and fix any type errors. Start `npm run dev` and verify the plan.md checklist: (1) reactivity — Blacksmith progress/inventory update each tick; (2) container — `<pre>` gone, IronMine/LumberMill generic, Market unchanged with controls; (3) 2D Blacksmith — 2 workers live, 5-second legibility passes; (4) 3D city-global — backdrop + money/citizens overlay, smooth (or documented cut); (5) bundle isolation — 2D path builds without `three`; (6) read-only — no state changes beyond the tick loop. Log findings. | Restrictions: Fix only type errors and obvious bugs — no refactors. | _Requirements: All_ | Success: `vue-tsc -b` exits clean; all applicable checklist items pass; no console errors during the normal game loop. Mark [-]/[x]._
