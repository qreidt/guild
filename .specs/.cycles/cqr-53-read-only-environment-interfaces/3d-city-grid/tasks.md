# Tasks — 3D City as a Grid-Based Map

> Sub-feature `3d-city-grid` of CQR-53. Build order follows the dependency chain
> **1 → 2 → 3 → (4, 5) → 6**. Tasks read [`requirements.md`](./requirements.md) (R1–R9)
> and [`plan.md`](./plan.md) in this folder. Mark each `[-]` when you start and `[x]`
> when done.
>
> **Gating risk:** the single-occupant assertion (Task 1/2) must pass for the real
> authored map before rendering (Task 3) — an assertion throw at module load blocks the
> whole 3D view. Author terrain + walls + buildings (Task 2) before houses/trees so
> collisions surface early.

- [x] 1. Grid model module — types, helpers, merge, assertion
  - File: `src/components/environment/views/city/grid.ts` (new)
  - Define `CELL = 3`, `GRID_MIN/MAX`, `Cell`, `Terrain`, `Occupant`, `PlacedOccupant`; implement `cellCenter`, `blockCenter`, `key`, `isBuildable`, `mergeHouses`, `buildOccupancy` per plan.md § grid.ts. Pure TS — no Vue, no `three`.
  - _Leverage: plan.md § Components → grid.ts; `BuildingID` from `src/game/city/buildings/common/Building.ts` (type only)_
  - _Requirements: R1, R2, R6.3, R6.4_
  - _Prompt: Implement this task for the 3d-city-grid sub-feature (read requirements.md + plan.md in `.specs/.cycles/cqr-53-read-only-environment-interfaces/3d-city-grid/` first). Role: TypeScript developer (pure model/algorithms) | Task: Create `grid.ts`. Export `CELL = 3`, `GRID_MIN = -16`, `GRID_MAX = 16`; types `Cell = readonly [number,number]`, `Terrain = 'grass'|'water'|'field'|'mountain'|'road'`, the `Occupant` discriminated union (`wall`{variant:'wall'|'tower', axis?:'x'|'z'} | `building`{id:BuildingID} | `house` | `house-dense`), and `PlacedOccupant = {anchor:Cell; w:number; d:number; occ:Occupant}`. Implement `cellCenter(i,j)=[i*CELL,j*CELL]`; `blockCenter(i,j,w,d)=[(i+(w-1)/2)*CELL,(j+(d-1)/2)*CELL]`; `key(i,j)`; `isBuildable(t)= t==='grass'`; `mergeHouses(cells)` — greedy 2x2 tiling over a Set, scanning ascending (j then i): when (i,j),(i+1,j),(i,j+1),(i+1,j+1) are all present & unconsumed emit a dense anchor at (i,j) and consume all four, else emit a single; return {dense:Cell[], singles:Cell[]}; `buildOccupancy(placed, terrainAt)` — expand each occupant to its w×d cells, throw `Error` naming the cell + both occupants if a cell is claimed twice, and throw if a building/house/house-dense cell is not buildable per terrainAt (walls/towers exempt); return Map<string,PlacedOccupant>. | Restrictions: Pure functions, deterministic, no Vue, no three, no side effects, no mutation of inputs. | _Leverage: plan.md interfaces_ | _Requirements: R1, R2, R6.3–R6.4_ | Success: `vue-tsc -b` passes; `mergeHouses` of a clean 2x2 yields one dense + zero singles; of a 3x2 yields one dense + two singles; `blockCenter(2,2,2,2)` = [7.5,7.5]; `buildOccupancy` throws on a duplicated cell. Mark [-]/[x]._

- [x] 2. Author the static map — terrain, walls, roads, building plots + assembly
  - File: `src/components/environment/views/city/town-layout.ts` (modify)
  - Refactor from continuous scatter to authored cell data: `terrainAt`, `WALL_TILES`, `ROAD_CELLS`, `BUILDING_PLOTS` (from the snapping table), and the one-time assembly that runs `buildOccupancy` (assertion). Keep `PALETTE`, `GROUND_SIZE`, `MOUNTAINS`, `GROUND_PATCHES` (sea + fields), the compass comment, and the PRNG helpers. **Houses/trees come in Tasks 4–5** — assemble buildings first so collisions surface.
  - _Leverage: plan.md § town-layout.ts; current `town-layout.ts` (PALETTE, GROUND_PATCHES, MOUNTAINS, mulberry32/pick); Task 1 `grid.ts`_
  - _Requirements: R3, R4, R5, R8, R2_
  - _Prompt: Implement this task for the 3d-city-grid sub-feature (read requirements.md + plan.md first). Role: TypeScript level-designer | Task: Refactor `town-layout.ts`. Keep `PALETTE`, `GROUND_SIZE`, `MOUNTAINS`, the compass comment block, and `mulberry32`/`pick`. Keep the sea + 2 fields in `GROUND_PATCHES` (drop the two path entries — roads become cells). Add: `terrainAt(i,j):Terrain` classifying water (sea, +x/south region), field (the two farm rects, -z/east), mountain (the 3 mountain footprints), road (the cells in ROAD_CELLS), else grass. Add `WALL_TILES:PlacedOccupant[]` — one wall tile per perimeter cell on i=±5 and j=±5 with axis tagged (north i=-5 axis 'z'; east j=-5 and west j=+5 axis 'x'; south i=+5 OPEN — no wall tiles), leaving the central gate cells empty (north gate (-5,0); east gate (0,-5); west gate (0,+5)), and tower tiles on the 4 corners (±5,±5) plus the gate-flanking bastions ((-5,±1),(±1,-5)). Add `ROAD_CELLS:Cell[]` = (0,0..5) ∪ (-4..4,0). Add `BUILDING_PLOTS` per the plan table: Market anchor (2,2), Blacksmith (-2,-1), LumberMill (-11,-1), IronMine (-10,-9), each with offset/scale/tone. Assemble `placed = [...WALL_TILES, ...buildingPlotsToPlaced(BUILDING_PLOTS)]` and export `OCCUPANCY = buildOccupancy(placed, terrainAt)`; export a partial `TOWN_MAP` ({occupants, roads: ROAD_CELLS minus building-covered cells, terrainPatches: GROUND_PATCHES, mountains: MOUNTAINS, trees: []}). | Restrictions: No Vue, no three. Data only. The module must assemble without the assertion throwing. Building cells must all be grass per terrainAt. | _Leverage: current town-layout.ts, grid.ts_ | _Requirements: R3, R4, R5, R8, R2_ | Success: `vue-tsc -b` passes; module loads without throwing; perimeter/gate/tower/road/building cells match the plan; the Blacksmith-covered road cells are excluded from TOWN_MAP.roads. Mark [-]/[x]._

- [x] 3. Render the scene from the grid (walls, towers, roads, hero buildings)
  - File: `src/components/environment/views/CityGlobalView3D.vue` (modify)
  - Replace the `WALL_BOXES` and path-patch loops with iteration over `TOWN_MAP`: wall/tower boxes via `blockCenter` + axis-aware sizing; per-cell road plates; hero buildings dispatched by `occ.id` at `blockCenter + offset`/`scale`. Keep camera, lights, fog, ground, mountains, overlay and legend. (Houses/trees render once Tasks 4–5 populate them — iterate those occupant kinds now so they light up automatically.)
  - _Leverage: current `CityGlobalView3D.vue` (camera/light/shadow/overlay/legend, HERO_MODELS); Task 2 `TOWN_MAP`; Task 1 helpers_
  - _Requirements: R3, R4, R5, R9_
  - _Prompt: Implement this task for the 3d-city-grid sub-feature (read requirements.md + plan.md first). Role: Vue 3 + TresJS developer | Task: Refactor `CityGlobalView3D.vue` to render from `TOWN_MAP`. Keep TresCanvas, camera, ambient/directional light + the onMounted shadow-camera setup, fog, grass ground, MountainMesh loop, the money/citizens overlay and the hero legend (still derived from props.view.buildings). Replace WALL_BOXES with a loop over occupants where occ.kind==='wall': for variant 'wall' render a TresMesh box at blockCenter(anchor,w,d) sized [CELL,H,T] when axis==='x' or [T,H,CELL] when axis==='z'; for variant 'tower' render a [towerW,towerH,towerW] box; reuse the current stone/towerStone colours and H/T/tower dims. Replace the path GROUND_PATCHES entries with a loop over TOWN_MAP.roads rendering a flat CELL×CELL plate (PALETTE.path) just above grass; keep sea+fields from TOWN_MAP.terrainPatches. For occ.kind==='building' look up the existing HERO_MODELS[occ.id] and the plot offset/scale and render at blockCenter+offset. Add (now, dormant until data exists) loops: occ.kind==='house' → HouseMesh at cellCenter; occ.kind==='house-dense' → HouseDenseMesh at blockCenter; TOWN_MAP.trees → TreeMesh at cellCenter+offset. | Restrictions: Read-only — render only from props.view + TOWN_MAP constants; no engine access; town must not rebuild per tick. three/@tresjs only here (as today). | _Leverage: existing component scaffolding, grid helpers_ | _Requirements: R3, R4, R5, R9_ | Success: with no tab selected the walled town renders with tiled walls, open gates/sea side, corner+bastion towers, tiled roads, and the 4 hero buildings near their old spots; overlay updates each tick; `vue-tsc -b` passes. Mark [-]/[x]._

- [x] 4. Houses — author cells, dense-merge, and the HouseDenseMesh
  - Files: `src/components/environment/views/city/HouseDenseMesh.vue` (new); `src/components/environment/views/city/town-layout.ts` (modify — add `HOUSE_CELLS`, wire merge into assembly)
  - Add `HOUSE_CELLS` approximating today's interior cluster (grass, inside walls, clear of plots/roads); run `mergeHouses` in the assembly and add the resulting `house-dense` (2×2) and `house` (1×1) occupants to `placed`. Build `HouseDenseMesh.vue` to depict 5+ dwellings in a 6×6 footprint.
  - _Leverage: Task 1 `mergeHouses`, `HouseMesh.vue`, `PALETTE`, `mulberry32`/`pick`_
  - _Requirements: R6, R2_
  - _Prompt: Implement this task for the 3d-city-grid sub-feature (read requirements.md + plan.md first). Role: Vue 3 + TresJS developer | Task: 1) `HouseDenseMesh.vue`: props position:[number,number,number], rotationY?, seed?; compose several HouseMesh instances at fixed local offsets within a 6×6 footprint (varied width/depth/height/colour from PALETTE.walls/roofs, optionally seeded) so it clearly reads as 5+ packed dwellings and is visually distinct from a single house and from the hero buildings. 2) In `town-layout.ts` add `HOUSE_CELLS:Cell[]` — author ~20–28 interior grass cells approximating the current house cluster (inside the walls, not on roads/plots/gates), including at least one clean 2×2 group so a dense block appears. In the assembly call mergeHouses(HOUSE_CELLS) and append dense anchors as {w:2,d:2,occ:{kind:'house-dense'}} and singles as {w:1,d:1,occ:{kind:'house'}} to `placed` BEFORE buildOccupancy. | Restrictions: House cells must be grass and not collide (assertion must pass). HouseDenseMesh is presentation-only; no engine access. | _Leverage: mergeHouses, HouseMesh, PALETTE_ | _Requirements: R6, R2_ | Success: the interior shows single houses plus ≥1 dense "5+" block where four houses meet; assertion passes; `vue-tsc -b` passes; the dense mesh is visually distinct. Mark [-]/[x]._

- [x] 5. Trees — author non-occupying decoration with clearance
  - File: `src/components/environment/views/city/town-layout.ts` (modify — add `TREE_DECOS`, filter into `TOWN_MAP.trees`)
  - Author `TREE_DECOS` (cell + sub-cell offset + scale) approximating today's northern forest, NW scatter, inner greenery and map edges; filter so only cells with no occupant and terrain grass/field survive, and the Mine/Mill plots keep a clearance ring.
  - _Leverage: current `buildTrees` scatter intent, `OCCUPANCY` from Task 2/4, `TreeMesh.vue`, `mulberry32`_
  - _Requirements: R7, R2.1_
  - _Prompt: Implement this task for the 3d-city-grid sub-feature (read requirements.md + plan.md first). Role: TypeScript level-designer | Task: In `town-layout.ts` add `TREE_DECOS:{cell:Cell; offset:[number,number]; scale:number}[]` reproducing the spirit of the current forest: a belt north of the town, a denser NW scatter toward the mountains, a little greenery inside the walls, and edge trees (skipping the sea side). A cell may carry more than one tree (vary offset). Then set TOWN_MAP.trees = TREE_DECOS filtered to: terrainAt(cell) is 'grass' or 'field', the cell is not in OCCUPANCY, and the cell is outside a small clearance ring around the LumberMill/IronMine plots. You may generate candidates with the existing mulberry32 seed then snap to cells. | Restrictions: Trees are NOT occupants — never add them to `placed`/buildOccupancy. No Vue, no three. Read-only data. | _Leverage: existing buildTrees, OCCUPANCY, mulberry32_ | _Requirements: R7, R2.1_ | Success: the forest/greenery survives the refactor; no tree overlaps a building/house/road/water cell; Mine and Mill keep tree-free clearance; `vue-tsc -b` passes. Mark [-]/[x]._

- [x] 6. Verify end-to-end (`vue-tsc -b` + manual checklist + assertion test)
  - Run the build typecheck and the dev server; exercise plan.md § Testing Strategy.
  - _Leverage: plan.md § Testing Strategy; `npm run build` (vue-tsc -b); `npm run dev`; optionally `npm run console` for the pure `mergeHouses`/`buildOccupancy` checks_
  - _Requirements: All_
  - _Prompt: Implement this task for the 3d-city-grid sub-feature (read requirements.md + plan.md first). Role: QA / TypeScript developer | Task: Run `npm run build` (vue-tsc -b) and fix any type errors. Start `npm run dev`, deselect all tabs to show the city, and verify the plan.md checklist: (1) walls form a tiled ring at ±15 with open N/E/W gates + open south + corner/bastion towers; (2) roads are tiled plates with no tile poking through the Blacksmith; (3) the 4 hero buildings sit within ~1 cell of their old spots and are centred in their plots (check LumberMill offset); (4) houses show singles + ≥1 dense "5+" block; (5) trees survive with no overlaps and Mine/Mill clearance; (6) duplicating a house cell onto an occupied cell makes the module throw at load (then revert); (7) money/citizens overlay updates each tick and the town does not rebuild; (8) three/@tresjs remain imported only by the 3D component + meshes. Optionally run the pure mergeHouses/buildOccupancy checks via tsx. Log findings. | Restrictions: Fix only type errors and obvious bugs — no scope creep. | _Requirements: All_ | Success: `vue-tsc -b` exits clean; all applicable checklist items pass; no console errors during the normal game loop. Mark [-]/[x]._
