# Tasks — 2D Blacksmith interior: vertical progress rows

> Sub-feature `2d-buildings-interface` of CQR-53. Tasks read
> [`requirements.md`](./requirements.md) (R1–R5) and [`plan.md`](./plan.md) in this
> folder. Mark each `[-]` when you start and `[x]` when done.
>
> Single-component refactor of `BlacksmithView2D.vue` — no view-model, registry, or
> engine change. Build order: **1 → 2 → 3 → (4)**. Task 4 (sparks) is optional and
> cut-able (R5). Verify live tick updates survive the rewrite (parent's #1 feasibility
> item) before considering it done.

- [ ] 1. Strip the smith figures, animations, and horizontal layout
  - File: `src/components/environment/views/BlacksmithView2D.vue` (modify)
  - Remove the per-smith figure group (head/apron/`bs-arm`/hammer), the per-smith anvil group, the in-SVG per-`cx` plaque/task/progress drawing, and the `cx`/`startX`/`gap` horizontal layout from the `smiths` computed. Delete the `.bs-arm`, `@keyframes bs-strike`, and `.bs-idle` figure rules from `<style>`. Keep the forge body + `bs-fire` flicker, the gradients, the header (name/funds), the inventory shelf, the `pct()` helper, the `prefers-reduced-motion` guard, and the `v-else` empty state. Leave a clean seam where the worker rows will go (Task 2). It is fine for the component to be temporarily figure-less but still compiling.
  - _Leverage: current `BlacksmithView2D.vue`; `GenericEnvironmentView.vue` (vertical-rows precedent)_
  - _Requirements: R1.6, R2.1, R2.2, R4_
  - _Prompt: Implement this task for the 2d-buildings-interface sub-feature (read requirements.md + plan.md in `.specs/.cycles/cqr-53-read-only-environment-interfaces/2d-buildings-interface/` first). Role: Vue 3 + SVG developer | Task: In `BlacksmithView2D.vue` delete everything that draws or animates the smith characters — the `<g v-for="s in smiths">` figure/anvil/plaque/progress drawn at per-smith `cx`, plus the `.bs-arm`, `@keyframes bs-strike`, and `.bs-idle` style rules — and drop the `cx`/`startX`/`gap` math from the `smiths` computed (you may remove the computed entirely; Task 2 iterates `view.workers` directly). KEEP: the `bs-wall`/`bs-fire`/`bs-glow` gradients, the forge body + ember glow + fire paths, the `bs-fire` flicker keyframes, the `prefers-reduced-motion` guard, the title+funds header, the inventory shelf, the `pct()` helper, and the `v-else` "No blacksmith data." branch. | Restrictions: Read-only — consume only the `view` prop; no engine access, no mutation, do NOT import `three`/`@tresjs`. No new `any`. | _Leverage: GenericEnvironmentView.vue_ | _Requirements: R1.6, R2.1, R2.2, R4_ | Success: `npx vue-tsc -b --force` passes; grep shows no `bs-strike`/`bs-arm`/figure markup/`cx` remain; the forge art + flicker, header, and inventory still render. Mark [-]/[x]._

- [ ] 2. Add the vertical worker-rows block
  - File: `src/components/environment/views/BlacksmithView2D.vue` (modify)
  - Render `view.workers` as a vertical HTML stack between the furnace banner and the inventory: one row per worker with `label`, the raw `task` identifier (italic "idle" when idle), a full-width progress bar (`width: pct(worker.progress)`), and a percent readout. Distinguish working vs idle by colour/emphasis. Handle 0/1/N workers (muted "No workers" when empty).
  - _Leverage: `GenericEnvironmentView.vue` (rows + `pct`), `WorkerView` from `src/modules/environment-view/types.ts`_
  - _Requirements: R1.1, R1.2, R1.3, R1.4, R1.5, R3.3_
  - _Prompt: Implement this task for the 2d-buildings-interface sub-feature (read requirements.md + plan.md first). Role: Vue 3 + Tailwind developer | Task: Add a `flex flex-col gap-2` worker block iterating `v-for="worker in view.workers" :key="worker.label"`. Each row: `worker.label` on the left; `worker.task ?? 'idle'` on the right (working → accent e.g. `text-green-400`/amber; idle → `text-gray-500 italic`); below, a progress track (`h-2 rounded bg-gray-700 overflow-hidden`) with a fill div `:style="{ width: pct(worker.progress) }"` (working → active fill, idle → muted) and the percent shown alongside/under. Render a muted "No workers" line when `view.workers.length === 0`. Place this block AFTER the furnace banner and BEFORE the inventory shelf, in `view.workers` order. Do not assume two workers; no fixed positions. | Restrictions: Use only `view.workers`/`pct`; the view-model already clamps progress to 0..1 — do not reintroduce un-clamped math; no new `any`; read-only. | _Leverage: GenericEnvironmentView.vue rows, WorkerView_ | _Requirements: R1.1–R1.5, R3.3_ | Success: working smiths show raw task + filling bar; idle smiths read as idle with a 0% bar and no stale task; renders for 0/1/2 workers; `npx vue-tsc -b --force` passes. Mark [-]/[x]._

- [ ] 3. Reframe the furnace as a backdrop banner above the rows + verify live updates
  - File: `src/components/environment/views/BlacksmithView2D.vue` (modify)
  - Slim the remaining forge SVG into a compact decorative banner (e.g. fixed-height `viewBox`, drop the figure-staging wall grid/floor) positioned above the worker rows; keep the `bs-fire` flicker. Confirm the overall vertical order is header → banner → worker rows → inventory, and that the layout reads cleanly. Then verify on the dev server that progress/inventory/funds update each tick and reduced-motion disables the flicker.
  - _Leverage: kept forge SVG from Task 1; `prefers-reduced-motion` guard_
  - _Requirements: R2.1, R2.2, R2.3, R2.4, R3.1, R3.2, R3.3, R1 (legibility, parent R3.5)_
  - _Prompt: Implement this task for the 2d-buildings-interface sub-feature (read requirements.md + plan.md first). Role: Vue 3 + SVG developer | Task: Convert the leftover forge SVG into a compact, decorative "furnace banner" (a small fixed-height SVG showing the forge body + ember glow + flickering fire, keeping the `bs-fire`/`bs-glow`/`bs-wall` gradients) and place it directly above the worker-rows block so the panel reads top-to-bottom as header → furnace banner → worker rows → inventory. Remove any wall-grid/floor geometry that only framed the old figures. Keep the banner decorative (it must encode no per-worker state). Then run the dev server, select the Blacksmith, and confirm: rows update each tick, a working bar fills, funds + inventory update, and toggling reduced motion stops the flicker. | Restrictions: Banner is presentation-only; read-only; no `three`; no new `any`. | _Leverage: forge SVG kept from Task 1_ | _Requirements: R2.1–R2.4, R3.1–R3.3, parent R3.5_ | Success: panel reads header→banner→rows→inventory; flicker present and reduced-motion-gated; live tick updates confirmed; 5-second legibility holds; `npx vue-tsc -b --force` passes. Mark [-]/[x]._

- [ ] 4. (Optional, cut-able) Anvil sparks ambient effect
  - File: `src/components/environment/views/BlacksmithView2D.vue` (modify)
  - Add a small ambient spark/ember effect (e.g. near an anvil motif in the banner), CSS-animated, gated by `prefers-reduced-motion`. Purely decorative. Skip entirely if time-constrained (R5.2) — R1–R4 do not depend on it.
  - _Leverage: existing `bs-fire` flicker pattern + reduced-motion guard_
  - _Requirements: R5.1, R5.2, R2.3_
  - _Prompt: Implement this task for the 2d-buildings-interface sub-feature (read requirements.md + plan.md first). Role: SVG/CSS motion developer | Task: Add a subtle ambient spark/ember effect in the furnace banner (a few small particles rising/fading near an anvil or the forge mouth) using CSS keyframes. It must encode no state and must be disabled under `prefers-reduced-motion: reduce` (extend the existing guard). Keep it cheap (a handful of elements, no per-frame JS). | Restrictions: Decorative only; read-only; reduced-motion-gated; no new `any`; no `three`. | _Leverage: bs-fire flicker + reduced-motion guard_ | _Requirements: R5.1, R5.2, R2.3_ | Success: sparks animate subtly and stop under reduced motion; no impact on legibility or tick updates; `npx vue-tsc -b --force` passes. This task may be skipped without affecting R1–R4. Mark [-]/[x]._
