# Tasks — Apothecary + City Expansion

> Build order follows the dependency chain **1 → 2 → 3 → 4 → 5 → 6 → 7 → 8 → 9**. Tasks read
> [`requirements.md`](./requirements.md) (R1–R7) and [`plan.md`](./plan.md) in this folder.
> Mark each `[-]` when you start and `[x]` when done.
>
> **Phase A (tasks 1–3) gates Phase B.** The Apothecary's plot at `[-8, 1]` does not exist
> until the town grows. Task 2 is the risk: it must be internally consistent in a single
> edit, and `buildOccupancy()` throws at module load if it is not.
>
> **The one thing that can crash the running game is task 5.** `chooseNextAction()` must
> validate before returning — see R4.6 and plan.md § Error Handling scenario 1.
>
> ---
>
> **All 9 tasks complete (2026-07-31, branch `feat/CQR-59`).** Typecheck and build clean;
> the console harness reproduces the R4.11 end state exactly; the dev server runs the full
> Phase A + Phase B checklist with no console errors. Deviations are recorded as as-built
> deltas on the affected clauses in [requirements.md](./requirements.md) — the notable one
> is R2.8 (west-band housing only, because R1.6's ≥ 40-anchor floor capped the budget at
> ~5 anchors out of 45).

- [x] 1. Shift the farm fields east (prep — clears the wall/farmland conflict)
  - File: `src/components/environment/views/city/town-layout.ts` (modify)
  - Move `FIELD_1` z −27.5…−18.5 → **−33.5…−24.5** and `FIELD_2` z −27…−19 → **−33…−25**, and move the matching `field-1` / `field-2` plates in `GROUND_PATCHES` to z centre **−29**. Done first and alone so the wall move in Task 2 lands on clean terrain.
  - _Leverage: `town-layout.ts:231-232` (the Rect constants), `town-layout.ts:427` (GROUND_PATCHES)_
  - _Requirements: R2.1, R2.2_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle. First read requirements.md and plan.md in `.specs/.cycles/cqr-59-apothecary-herbs-and-potions/`. Role: TypeScript developer (authored level data) | Task: In `town-layout.ts`, change `FIELD_1` to `{ x0: -15, x1: 1, z0: -33.5, z1: -24.5 }` and `FIELD_2` to `{ x0: 3, x1: 15, z0: -33, z1: -25 }` — same x extents, same sizes, shifted ~6 units east (more negative z). Then update the two hardcoded plates in `GROUND_PATCHES`: `field-1` position `[-7, 0.04, -23]` → `[-7, 0.04, -29]` and `field-2` position `[9, 0.04, -23]` → `[9, 0.04, -29]`; leave their `size` values alone. | Restrictions: Do not touch `SEA`, walls, roads or any plot yet. The `Rect` constants and the `GROUND_PATCHES` plates are two independent sources of truth for the same rectangles — if they disagree, terrain reports grass where the ground renders brown. Change both or neither. | _Leverage: existing Rect + GroundPatch shapes_ | _Requirements: R2.1, R2.2_ | Success: `npx vue-tsc -b --force` clean; the dev server's 3D city view still mounts (proves `buildOccupancy()` passed); the farm plates visibly sit further east and the IronMine at `[-10, -9]` is unaffected. Mark this task [-] before starting and [x] when done._

- [x] 2. Expand the town — bounds, walls, gates, roads, trail, LumberMill relocation
  - File: `src/components/environment/views/city/town-layout.ts` (modify)
  - The core geometry change, in **one internally-consistent edit**: replace `TOWN_HALF_CELLS` with per-axis bounds (interior `i −8…4`, `j −6…6`), rewrite `buildWallCells()` for a rectangle, extend `ROAD_CELLS`, rebuild `TRAIL_CELLS`, and move the LumberMill anchor + `LUMBER_CENTER`.
  - _Leverage: `town-layout.ts:132` (TOWN_HALF_CELLS), `:157` (BUILDING_PLOTS), `:204` (ROAD_CELLS), `:211` (TRAIL_CELLS), `:259` (buildWallCells), `:472` (LUMBER_CENTER); `grid.ts:126` (buildOccupancy — the load-time gate)_
  - _Requirements: R1.1–R1.5, R1.7, R1.8, R2.3–R2.6_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: TypeScript developer (authored level data / grid geometry) | Task: In `town-layout.ts`: 1) Replace `const TOWN_HALF_CELLS = 5` with per-axis interior bounds — `i` −8…4 and `j` −6…6 — plus derived wall lines N `i=-9`, S `i=+5`, E `j=-7`, W `j=+7`. Keep them module-private. 2) Rewrite `buildWallCells()` for a rectangle, returning the same `WallCell[]` shape: four corner towers at (−9,−7) (−9,+7) (+5,−7) (+5,+7); north wall along `i=-9` for j −6…6 skipping the gate at j=0, bastion towers at |j|=1, `axis: "z"`; east wall along `j=-7` for i −8…4 skipping the gate at i=0, bastions at |i|=1, `axis: "x"`; west wall along `j=+7` for i −8…4 skipping the gate at i=0, no bastions; south (i=+5) gets no wall tiles, corners only. 3) `ROAD_CELLS`: row i=0 for j −6…6, plus column j=0 for i −8…4 skipping (0,0). 4) `TRAIL_CELLS`: i = −9, −10, −11 at j=0 — and no further. 5) `BUILDING_PLOTS.LumberMill.anchor` `[-11,-1]` → `[-13,-1]`, and `LUMBER_CENTER` → `blockCenter(-13, -1, 2, 2)`. | Restrictions: Make this one atomic edit — a half-moved wall leaves an unreachable mill and a trail starting inside the walls. The trail must stop at i=−11 because the relocated mill covers (-13,0) and (-12,0); an overlap will NOT throw (terrainAt returns "grass" for building cells before it checks roads) but will z-fight two plates at y=0.03. Do not change `GROUND_SIZE`, the compass convention, the `SEA` rect, or `grid.ts`. Do not add the Apothecary plot yet — that is task 7. | _Leverage: existing WallCell/BoxRect shapes; buildOccupancy as the correctness net_ | _Requirements: R1.1–R1.5, R1.7, R1.8, R2.3–R2.6_ | Success: `npx vue-tsc -b --force` clean; the 3D city view mounts (buildOccupancy passed); the enlarged ring renders with three gates and corner towers and the south open to the sea; the LumberMill has clearance from the north wall and is still trail-connected; no trail plate renders under the mill or over a road tile. Mark [-]/[x]._

- [x] 3. Dress the new band — trees and housing
  - File: `src/components/environment/views/city/town-layout.ts` (modify)
  - Retune `buildTrees()` for the new extents and author a modest house cluster in the west / north-west band, leaving the north-gate area open.
  - _Leverage: `town-layout.ts:308` (HOUSE_CELLS), `:488` (buildTrees), `grid.ts:89` (mergeHouses)_
  - _Requirements: R1.6, R2.7, R2.8, R2.9_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: TypeScript developer / level designer | Task: In `town-layout.ts`: 1) Retune `buildTrees()` for the enlarged interior. Two bands assume the old extents: the interior-greenery band samples world ±12 (cells ±4), which now covers only the middle of a 13×13 interior; and the forest belt samples x −23…−34, i.e. cells i −8…−11 — cells −8 and −9 now sit INSIDE the new north wall. Widen the interior band and pull the forest belt north so trees do not sprout inside the town. 2) Extend `HOUSE_CELLS` with a modest cluster in the new west / north-west band so the expansion does not read as bare grass. 3) Then measure free 2×2 interior anchors (a temporary log is fine — remove it before finishing) and confirm at least 40, including `[-8, 1]`. | Restrictions: HOUSE_CELLS must NEVER claim (-8,1), (-7,1), (-8,2) or (-7,2) — that is the reserved Apothecary plot. Leave the area near the north gate open for future buildings generally; this is dressing, not a fill-every-cell exercise. Houses must land on grass or buildOccupancy throws at module load. Keep the southern band (i ≥ 3) open toward the sea as the existing tree filter already does. | _Leverage: mergeHouses greedy 2×2 tiling, existing HOUSE_CELLS quadrant comments, treeCellOk_ | _Requirements: R1.6, R2.7, R2.8, R2.9_ | Success: `npx vue-tsc -b --force` clean; the 3D view mounts; no trees inside the walls where the old forest belt was; the new band looks inhabited rather than empty; ≥ 40 free 2×2 anchors measured, `[-8, 1]` among them; the temporary measurement log is removed. Mark [-]/[x]._

- [x] 4. Herb + potion item catalog (33 herbs, 2 potions)
  - Files: `src/modules/items/id.ts` (modify), `src/modules/items/values/goods.ts` (modify), `src/modules/items/registry.ts` (modify)
  - 35 `ItemID` entries, 35 stackable `Item` subclasses, 35 `ItemRegistry` mappings. Mechanical, but `ItemRegistry` is typed `Record<ItemID, IItem>` so any omission is a compile error.
  - _Leverage: `values/goods.ts` (Item subclass shape + default-export literal), `registry.ts` (mapping style), `id.ts` (`// Weapons` / `// Armors` grouping style)_
  - _Requirements: R3.1–R3.8_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: TypeScript developer | Task: Add all 33 herbs plus `HealthPotion` and `ManaPotion` using the exact table in `request.md` § Herb catalog (names and values are authoritative — do not improvise). 1) `id.ts`: 35 enum entries in two new commented groups `// Herbs` and `// Potions`, matching the existing `// Weapons` / `// Armors` style. Key and value identical, PascalCase (e.g. `IronbarkMoss = 'IronbarkMoss'`). 2) `values/goods.ts`: 35 classes extending `Item`, each with `static id`, `static name` (the DISPLAY name — so `IronbarkMoss` has `static name = 'Ironbark Moss'`), `static value`, `static weight = 1`. Add every one to the default-export object literal, which grows from 4 to 39 entries. 3) `registry.ts`: 35 `[ItemID.X]: Goods.X` mappings under `// Herbs` / `// Potions` headings. Both potions: value 20, weight 1. | Restrictions: These must extend `Item` (stackable), NOT `EquippableItem` — non-stackables commit into `stacks` but `getCount()` reads `instances`, so the two disagree. Extend the existing `goods.ts`; do NOT create `herbs.ts` / `potions.ts` or a barrel file (explicit user decision). Tier is descriptive only — it must not appear in code. 31 of the 33 herbs will have no producer and no consumer; that is expected and matches the existing unused `Leather*` entries. | _Leverage: existing Item subclasses, ItemRegistry Record type_ | _Requirements: R3.1–R3.8_ | Success: `npx vue-tsc -b --force` clean (a missing registry mapping is a compile error, so a clean typecheck proves all 35 are wired); `npm run console -- give BlackSmith Bloodroot 5` succeeds and reports the item; spot-check that `Ironbark Moss`, `Health Potion` and `Mana Potion` render their display names with spaces. Mark [-]/[x]._

- [x] 5. Apothecary building + two brewing actions
  - Files: `src/game/city/buildings/Apothecary.ts` (new), `src/game/city/buildings/common/Building.ts` (modify), `src/game/city/City.ts` (modify)
  - `BuildingID.Apothecary`, the building class with 2 workers and a seeded inventory, two brew actions, and registration in the city. **`chooseNextAction()` must validate before returning or the tick loop dies.**
  - _Leverage: `BlackSmith.ts:18` (InventoryAccountService.init seeding), `:37` (priority-list chooseNextAction), `:114` (MakeIronSwordAction — the correct Action template), `common/Action.ts`, `common/Worker.ts`_
  - _Requirements: R4.1–R4.11_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: TypeScript game-engine developer | Task: 1) Add `Apothecary = 'Apothecary'` to the `BuildingID` enum. 2) Create `Apothecary.ts` per plan.md § Components — `level = 1`, `money = 100`, `static building_id`, 2 `Worker`s, and `InventoryAccountService.init(BuildingID.Apothecary, { stacks: new Map([[ItemID.Bloodroot, 10], [ItemID.Manabloom, 10]]) })`. 3) `chooseNextAction()`: read `this.inventory.getCountByGoodId()` fresh, walk a priority-ordered production list `{ HealthPotion: {desired_amount: 3, action: BrewHealthPotionAction}, ManaPotion: {desired_amount: 3, action: BrewManaPotionAction} }`; for each entry below its target, construct the action and call `this.inventory.validateLedger(action.input!)` — return it ONLY on success, otherwise `continue`; fall through to `return new WaitAction()`. 4) Two `Action` subclasses: `total_ticks = 12`, `static building_id`, `static input_origin = BuildingID.Apothecary`, instance `input` (Bloodroot 3 / Manabloom 3), instance `output_destination = BuildingID.Apothecary`, instance `output` (HealthPotion 1 / ManaPotion 1), and `static name = 'BrewHealthPotion'` / `'BrewManaPotion'`. 5) Register in the `City.ts` `buildings` Map with its import. | Restrictions: CRITICAL — never return an action whose input is not validated. `Action.start()` calls `createTransaction()`, which THROWS `InsufficientTransactionContentsError`, and nothing catches it up through `handleTick` → `City.handleTick` → `nextTick`; the tick loop dies. Declare `output` and `output_destination` as INSTANCE properties, not statics: `Action` declares `public output = null`, so a `static output` (as in `MakeIngotAction`) leaves the instance null and the transaction outputs nothing — copy `MakeIronSwordAction`, not `MakeIngotAction`. No `BuyFromMarketAction` (the building has no income, so the condition can never become true). No `TransportAction` / market line. No `shouldTick()` override — brewing is indoor work and is not night-gated. Do NOT restructure `BaseBuilding.handleTick`: worker A's `start()` must debit the ledger before worker B chooses, which is what makes the `continue` branch reachable. | _Leverage: BlackSmith as the template, minus the market-buy preamble and the make_ingot fallback_ | _Requirements: R4.1–R4.11_ | Success: `npx vue-tsc -b --force` clean. In `npm run console`: `buildings` lists the Apothecary with 2 workers; `inspect Apothecary` shows Bloodroot 10 / Manabloom 10; `tick 40` then `inspect Apothecary` gives exactly `HealthPotion 3`, `ManaPotion 3`, `Bloodroot 1`, `Manabloom 1`, both workers on `WaitAction`, money unchanged; `tick 200` throws nothing; `market` shows no potion in stock or trades. Mark [-]/[x]._

- [x] 6. 2D Apothecary interior + theme + worker label
  - Files: `src/components/environment/BuildingInterior2D.vue` (modify), `src/components/environment/views/ApothecaryView2D.vue` (new), `src/components/environment/environment-registry.ts` (modify), `src/modules/environment-view/environment-view.ts` (modify)
  - A 4th accent theme, the themed interior on the shared shell, its registry entry, and the `'Herbalist'` worker label prefix.
  - _Leverage: `IronMineView2D.vue` (closest template — banner SVG + reduced-motion-guarded animation), `BuildingInterior2D.vue:13` (ThemeName union + THEMES), `environment-registry.ts:15`, `environment-view.ts:23` (WORKER_LABEL_PREFIX)_
  - _Requirements: R5.1–R5.8_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: Vue 3 + SVG/CSS UI developer | Task: 1) In `BuildingInterior2D.vue` add `'violet'` to the `ThemeName` union and a matching `THEMES.violet` entry — `funds: 'bg-violet-900/40 border-violet-700 text-violet-300'`, `task: 'text-violet-300 font-medium'`, `bar: 'bg-violet-500'`, `count: 'text-violet-300'`. 2) Create `ApothecaryView2D.vue` modelled on `IronMineView2D.vue`: a `view: EnvironmentView | null` prop passed to `BuildingInterior2D` with `theme="violet"`, a funds icon (e.g. `⚗`), an empty message, and a `#banner` SVG (viewBox `0 0 640 140`, `role="img"`, `aria-label`) depicting an apothecary workshop — shelved bottles, mortar and pestle, a bubbling still. 3) Register it in `environment-registry.ts` as `defineAsyncComponent(() => import('./views/ApothecaryView2D.vue'))`. 4) Add `[BuildingID.Apothecary]: 'Herbalist'` to `WORKER_LABEL_PREFIX`. | Restrictions: Strictly read-only — render from the `view` prop alone, never touch engine singletons, no buttons that mutate state. The banner is decorative and must encode NO worker state. Any ambient animation must be guarded by `@media (prefers-reduced-motion: reduce)`, as the siblings do. Tailwind only sees class strings it can find in source, so write the theme classes out in full — no interpolation or dynamic construction. Do not add a `static name` to the building class; `mapEnvironmentView` falls back to the JS class name, which is already `Apothecary`. | _Leverage: BuildingInterior2D shell, IronMineView2D structure_ | _Requirements: R5.1–R5.8_ | Success: `npx vue-tsc -b --force` clean; selecting the Apothecary opens the themed interior (not `GenericEnvironmentView`); workers read "Herbalist 1" / "Herbalist 2"; task labels read "BrewHealthPotion" / "BrewManaPotion" and progress bars fill live; inventory and funds render; no console errors. Mark [-]/[x]._

- [x] 7. 3D hero mesh + city plot
  - Files: `src/components/environment/views/city/ApothecaryMesh.vue` (new), `src/components/environment/views/CityGlobalView3D.vue` (modify), `src/components/environment/views/city/town-layout.ts` (modify)
  - The mesh, its `HERO_MODELS` entry, and the `BUILDING_PLOTS` anchor at `[-8, 1]` — added together so the plot never renders empty.
  - _Leverage: `city/BlacksmithMesh.vue` and `city/LumberMillMesh.vue` (authoring style), `CityGlobalView3D.vue:73` (HERO_MODELS), `town-layout.ts:157` (BUILDING_PLOTS), `PALETTE`_
  - _Requirements: R6.1–R6.6_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: Vue 3 + Three.js developer | Task: 1) Create `city/ApothecaryMesh.vue` from `three` primitives in the style of `BlacksmithMesh.vue`, sized for a 2×2 plot (6×6 world units) and drawing colours from `PALETTE`. Suggested silhouette: a timbered shop with a steep roof, a chimney with a small still, a hanging shop sign — it must read at the fixed 45° camera distance. 2) In `CityGlobalView3D.vue` (note: this file is at `views/`, NOT `views/city/`) add the static import and the `HERO_MODELS` entry for `BuildingID.Apothecary`. 3) In `town-layout.ts` add `[BuildingID.Apothecary]: { anchor: [-8, 1], offset: [0, 0], scale: 1.0, tone: <an herbal green or violet hex> }` to `BUILDING_PLOTS`. | Restrictions: `three` / `@tresjs/core` may be imported ONLY inside the `.vue` mesh component — never in `town-layout.ts` or `grid.ts`. Add the `BUILDING_PLOTS` entry and the `HERO_MODELS` entry in the SAME task: a plot without a model renders empty (`heroModel(id)` returns undefined). The anchor covers cells (-8,1) (-7,1) (-8,2) (-7,2) and needs no trail extension — it abuts the extended j=0 road directly. If task 3 authored a house onto any of those cells, `buildOccupancy()` will throw at module load naming both occupants; fix the house cells, not the anchor. | _Leverage: existing hero meshes, HERO_PLOTS derivation (no render-loop change needed)_ | _Requirements: R6.1–R6.6_ | Success: `npx vue-tsc -b --force` and `npm run build` clean; the Apothecary renders as its own mesh immediately inside the north gate, abutting the road; the 3D view still mounts (buildOccupancy passed); `three` stays out of the 2D and initial bundles. Mark [-]/[x]._

- [x] 8. Documentation reconciliation + as-built deltas
  - Files: `.specs/roadmap.md` (modify), `.specs/apothecary-herb-catalog.md` (modify), `.specs/.cycles/README.md` (modify), this cycle's specs (annotate), `CLAUDE.md` (check)
  - Rename the roadmap entry, close the herb-catalog's answered questions, and record any deviation from this spec as an as-built delta rather than a rewrite.
  - _Leverage: `.specs/roadmap.md:52`, `.specs/apothecary-herb-catalog.md` § Open / not yet decided, the CQR-53 cycle's as-built delta style (see its `requirements.md` R5.2 blockquote)_
  - _Requirements: R7.1–R7.4_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: Technical writer / maintainer | Task: 1) `.specs/roadmap.md:52`: "Alchemist's Lab" → "Apothecary". 2) `.specs/apothecary-herb-catalog.md`: its "Open / not yet decided" section lists four questions — "which herbs brew into which potions", "whether an intermediate essence stage sits between herb and potion", "how many potion lines ship in v1", "which subset of the catalog actually gets ItemID entries in the first cycle". All four are now answered (Bloodroot→Health, Manabloom→Mana; no essence stage, deferred; two lines; the full catalog). Mark them resolved with a pointer to this cycle rather than deleting the section. 3) Check the CQR-53 `3d-city-grid` docs and the compass comment at the top of `town-layout.ts` for statements the expansion invalidates — notably the 9×9 interior and `TOWN_HALF_CELLS` — and correct or annotate them. 4) Update `.specs/.cycles/README.md` to reflect the delivered status. 5) Record any implementation deviation from this spec as an as-built delta — a dated note on the affected requirement in `requirements.md` — not by rewriting the original brief. | Restrictions: Per CLAUDE.md, keep specs in sync by noting deviations, NOT by editing the original request to match what was built. `request.md` and `city-expansion/request.md` are historical intake and stay as-is. Base every claim on what actually shipped. | _Leverage: CQR-53's as-built delta blockquote style_ | _Requirements: R7.1–R7.4_ | Success: The roadmap names the Apothecary; the herb catalog's open questions are marked resolved with a pointer; no doc still asserts a 9×9 interior or `TOWN_HALF_CELLS`; any deviation is recorded as a delta. Mark [-]/[x]._

- [x] 9. Verify end-to-end
  - Run the typecheck, the build, the console harness and the manual checklist in plan.md § Testing Strategy.
  - _Leverage: plan.md § Testing Strategy, `npx vue-tsc -b --force`, `npm run build`, `npm run console`, `npm run dev`_
  - _Requirements: All_
  - _Prompt: Implement this task for the apothecary-herbs-and-potions cycle (read requirements.md + plan.md first). Role: QA / TypeScript developer | Task: 1) Run `npx vue-tsc -b --force` (the `-b` flag is incremental — without `--force` it can no-op and print nothing after edits) then `npm run build`; fix any errors. 2) Interactive console (`npm run console`, NOT the one-shot form — that dispatches a single command and exits, so it can never reach the post-tick state): `buildings`, `inspect Apothecary`, `tick 40`, `inspect Apothecary` (expect HealthPotion 3, ManaPotion 3, Bloodroot 1, Manabloom 1, both workers on WaitAction, money unchanged), `tick 200` (expect no throw, state unchanged), `market` (expect no potion in stock or trades). 3) `npm run dev` and walk the full plan.md § Manual verification checklist — Phase A items 1–6 and Phase B items 7–13. Log findings. | Restrictions: Fix only type errors and genuine bugs — no refactors, no scope additions. If the end-state trace does not match R4.11 exactly, diagnose before adjusting the expected numbers: a mismatch usually means `chooseNextAction()` is validating in the wrong place or `output` was declared static. | _Requirements: All_ | Success: Typecheck and build both clean; every checklist item passes or is logged with a reason; no console errors during a normal run; the R4.11 end state matches exactly. Mark [-]/[x]._
